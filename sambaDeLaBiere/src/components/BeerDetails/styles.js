import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {},
  banner: {
    width: 90,
    height: 200,
    resizeMode: 'contain',
    alignSelf: 'center',
  },
  infoContainer: {
    alignItems: 'center',
  },
  ingredientsContainer: {
    alignItems: 'center',
  },
});
