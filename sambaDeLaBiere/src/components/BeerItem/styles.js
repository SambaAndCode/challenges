import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginBottom: 10
  },
  image: {
    height: 90,
    width: 90,
    borderRadius: 10
  }
})